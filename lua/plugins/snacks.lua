return {
  "folke/snacks.nvim",
  opts = {
    indent = {
      indent = {
        -- Enable indent guide only in the current scope and onwards.
        -- Previous scopes on the left will use listchars to show whitespace.
        only_scope = true,
      },
    },
  },
}
